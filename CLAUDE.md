# CLAUDE.md

This file provides guidance to AI assistants working in this repository.

## Project Overview

**Claude-code** is a newly initialized repository. It currently contains only scaffolding files. As the project develops, this document should be updated to reflect the actual structure, tooling, and conventions in use.

## Repository Structure

```
Claude-code/
├── README.md        # Project readme
└── CLAUDE.md        # This file — AI assistant guidance
```

## Development Workflow

No build system, test framework, or linting configuration has been set up yet. When these are added, document:

- **Build**: build tool and commands (e.g., `npm run build`)
- **Test**: test framework and commands (e.g., `npm test`)
- **Lint/Format**: linting and formatting tools and commands (e.g., `npm run lint`)

## Conventions

- Keep this file up to date as the project evolves.
- When adding new tooling (package manager, test framework, CI/CD), update the relevant sections above.

## Notes for AI Assistants

- Read this file at the start of every session to understand current project state.
- Before making changes, verify the current directory structure and any configuration files that may have been added since this file was last updated.
- Prefer minimal, focused changes. Do not add unnecessary abstractions or tooling.
